name: pi
display: pi

detect:
  commands: [pi]
  env: RAT_PI

options:
  model:
    arg: --model
    description: Model pattern or ID
  provider:
    arg: --provider
    description: Provider name
  thinking:
    arg: --thinking
    enum: [off, minimal, low, medium, high, xhigh]
    description: Thinking level

kernel:
  type: tmux
  command: "{runtime} {opts} --extension {bridge} --session-dir {data_dir}/sessions"
  bridge: bridge.ts
  submit: Enter
  cancel: Escape

frontend:
  type: tmux

install:
  check_commands: [tmux]
  runtime:
    manager: npm
    deps: ["@mariozechner/pi-coding-agent"]
  smoke:
    ctl: status
